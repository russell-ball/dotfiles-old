- clean:
    - "~"

- link:

    # shell configuration
    ~/.shellrc: shellrc/
    ~/.bashrc:
      force: true
      path: shellrc/bashrc
    ~/.bash_profile:
      force: true
      path: shellrc/bash_profile
    ~/.inputrc:
      force: true
      path: shellrc/inputrc

    # regular dotfiles
    ~/.ackrc: .ackrc
    ~/.ctags: .ctags
    ~/.gitconfig: .gitconfig
    ~/.gitignore: .gitignore
    ~/.gitmodules: .gitmodules
    ~/.hushlogin: .hushlogin
    ~/.irbrc: .irbrc
    ~/.pryrc: .pryrc
    ~/.screenrc: .screenrc
    ~/.tmux.conf: .tmux.conf
    ~/.wgetrc: .wgetrc
    ~/.mackup: .mackup/
    ~/.mackup.cfg: .mackup.cfg

    # vim
    ~/.vim:
      force: true
      path: vimfiles/
    ~/.vimrc:
      force: true
      path: vimfiles/vimrc
    ~/.gvimrc:
      force: true
      path: vimfiles/gvimrc

    # custom scripts
    ~/bin: bin/

    # Ruby configuration
    ~/.gemrc: .gemrc
    ~/.rbenv/default-gems:
      create: true
      force: true
      path: rbenv/default-gems

- shell:
    - [ "git submodule update --init --recursive", "Installing submodules" ]
    - [ "cd vimfiles && git pull origin master", "Getting latest vim configuration" ]
    - [ "vim +PlugInstall +qall", "Installing vim plugins" ]
